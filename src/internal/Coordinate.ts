export class Coordinate {
    x: number;
    y: number;
    distance: number;
    constructor(x : number, y : number, distance : number) {
        this.x = x;
        this.y = y;
        this.distance = distance;
    }
}